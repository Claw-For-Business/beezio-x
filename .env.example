# X (Twitter) API v2 — read (fetch tweets)
# Create an app at https://developer.x.com/ and use the Bearer Token from your project.
X_BEARER_TOKEN=your_bearer_token_here

# X API — write (post replies). Same app: use API Key & Secret, then generate Access Token & Secret.
X_API_KEY=your_api_key_here
X_API_SECRET=your_api_secret_here
X_ACCESS_TOKEN=your_access_token_here
X_ACCESS_TOKEN_SECRET=your_access_token_secret_here
